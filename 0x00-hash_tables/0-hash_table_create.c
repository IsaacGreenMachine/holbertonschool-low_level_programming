#include "hash_tables.h"
hash_table_t *hash_table_create(unsigned long int size){
struct hash_table_t *array = NULL;

/*for (int i = 0; i<size; i++) {
array[i] =
}

hash_table_t test = {size, };

return &test;


// create an array of size size, for length of size, malloc pointer in each slot. return pointer to array

*/
}
